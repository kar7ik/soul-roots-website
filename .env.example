# Copy this file to .env for local `netlify dev`, and set the same variable
# in the Netlify dashboard under Site settings > Environment variables for deploys.
GOOGLE_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
